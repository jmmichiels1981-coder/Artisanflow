<analysis>
IMPORTANT — Correction du contexte avant fork :

1. L’harmonisation UI n’est PAS appliquée dans deux pages.

Contrairement au résumé précédent, les composants suivants n’apparaissent pas du tout dans :
- /devis/creer/dictee-vocale-structuree-par-ia.jsx
- /devis/creer/assiste-par-ia.jsx

Composants manquants :
- AcompteDisplay (affichage de l’acompte automatic)
- BankingInfoDisplay (affichage des informations bancaires)
- DocumentsSection (section des documents PDF)

Ils apparaissent uniquement dans manuel.jsx.

Même dans la preview interne (localhost:3000), ces trois composants n’apparaissent pas.  
Ce n’est donc **ni un problème de cache**, ni un problème de CDN :  
➡️ le code n’a tout simplement pas été intégré dans ces deux fichiers.

2. Le pipeline de prévisualisation externe est cassé côté Emergent.

Ce problème n’a rien à voir avec le navigateur.  
Même après :
- build complet,
- purge,
- redémarrage des services,
- mise à jour de version.json,

➡️ L’URL de preview externe continue d’afficher un ancien bundle.

L’ingénieur Emergent a confirmé qu’il s’agit d’un problème d’infrastructure.  
Cela empêche toute validation externe du travail effectué.

3. La correction annoncée par l’agent IA n’a pas été appliquée.

L’agent affirme avoir déplacé les composants dans les deux pages, mais :
- rien n’apparaît dans l’UI,
- rien n’est visible dans la preview interne,
- les blocs ne sont présents que dans la page Manuel.

Il faudra donc refaire correctement l’intégration dans ce fork.

4. Tâche prioritaire de ce fork :

Réouvrir les deux fichiers :
- dictee-vocale-structuree-par-ia.jsx
- assiste-par-ia.jsx

Puis ajouter les trois composants EXACTEMENT au même emplacement que dans manuel.jsx :
- juste après le bloc Total HT / TVA / TTC
- avant les boutons d'action
- même design
- même ordre
- ne pas les placer à l’intérieur de conditions comme {isStructured} ou {step === 3}

Cette fois, vérifier visuellement dans la preview interne que les trois composants apparaissent bien.

5. À propos de la validation dans la preview externe :

Tant que l’équipe DevOps n’a pas réparé la pipeline externe,
➡️ toutes les validations doivent être faites UNIQUEMENT dans la preview interne (localhost:3000).

Ne pas se baser sur l’URL de preview externe tant que la pipeline n’est pas réparée.

</analysis>
